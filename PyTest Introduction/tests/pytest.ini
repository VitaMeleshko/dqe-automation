[pytest]
# marks
# custom marks
markers =
    validate_csv: Tests for CSV validation
    unmarked: Tests without explicit marks
    parquet_data: Tests for Parquet file validation
# python_files
python_files = test_*.py
# addopts
addopts =
         --strict-markers
         --tb=short
         --html=reports/report.html