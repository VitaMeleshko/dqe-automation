[pytest]
# marks
# custom marks
markers =
    parquet_data: Tests for Parquet file validation

# python_files
python_files = test_*.py
# addopts
addopts =
         --strict-markers
         --tb=short
         --html=reports/report.html